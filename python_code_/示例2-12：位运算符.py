# 按位与运算 &, 对应数位都为1时为1，否则为0
print(12 & 8)

# 按位或运算 |，对应数位上有一个为1时为1，否则为0
print(4 | 8)

# 按位异或 运算 ^，对应数位相同为0，不同为1
print(31 ^ 32)

# 按位取反运算  ~, 对数位取反
print(~123)

# 左移位 <<, 左移n位，相当于乘以2的n次方
print(2 << 3)

# 右移运算 >>, 右移n位，相当于除以2的n次方
print(8 >> 2)
